# 보안 테스트 지침 — 260827-seed-ux-bugfix

## devsecops 관점 검토
- **인가**: `GET /api/enrollments/mine`는 인증된 호출자 본인 신청만 반환하는 기존 엔드포인트다. FR-2는 이를 재사용하며 신규 엔드포인트/권한 변경 없음. 멘티가 아닌 사용자에게는 호출 자체를 하지 않는다.
- **데이터 노출**: 목록 응답에 타인 신청 정보를 추가하지 않는다(본인 신청만 조회). 신청 상태(`APPLIED`) 외 민감정보 미노출.
- **시드 데이터**: `V12__seed_demo.sql`은 개발/데모 픽스처(공통 비밀번호 `password123`)로, 이미 운영 배포 전 정리 대상으로 문서화되어 있다. 이번 변경은 비밀번호/계정을 추가하지 않는다.
- **주입/이스케이프**: SQL 변경은 정적 리터럴 값(4,4)·주석뿐으로 사용자 입력 미포함.

## 결론
신규 보안 위험 없음. 별도 보안 테스트 추가 불필요(N/A).
